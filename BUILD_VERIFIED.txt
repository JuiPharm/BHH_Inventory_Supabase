BHH Inventory Netlify Complete v1.2 FIXED

Build verification performed in container before removing node_modules/dist:
- npm install --ignore-scripts
- npm run build

Result:
- Vite production build passed.

Important deployment note:
- package-lock.json is intentionally excluded because this environment writes internal registry URLs into lockfiles.
- Netlify should install dependencies from package.json directly.
